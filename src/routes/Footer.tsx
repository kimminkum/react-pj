interface FooterProps {
  windowWidth: number;
  onToggle: () => void;
}

const Footer: React.FC<FooterProps> = ({ windowWidth }) => {
  return (
    <>
      <div></div>
    </>
  );
};

export default Footer;
